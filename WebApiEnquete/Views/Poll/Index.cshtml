@model IEnumerable<WebApiEnquete.Models.poll>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.poll_description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.poll_description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.poll_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.poll_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.poll_id })
        </td>
    </tr>
}

</table>
